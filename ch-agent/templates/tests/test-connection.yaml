apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ch-agent.fullname" . }}-test-connection"
  labels:
    {{- include "ch-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ch-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
